language: rust
rust:
  - stable

script:
  - cargo test

after_success:
  - cargo install cargo-bump
  - cargo bump
  - git config --global user.email "builds@travis-ci.com"
  - git config --global user.name "Travis CI"
  - git push -q https://$GITHUB_TOKEN@github.com/tomorrow-paper/paper